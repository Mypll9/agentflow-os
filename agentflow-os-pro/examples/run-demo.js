import { runWorkflow } from "../src/workflows/softwareEngineeringWorkflow.js";

await runWorkflow(
  "Create an autonomous engineering workflow that can plan, implement, review, and validate code changes."
);
